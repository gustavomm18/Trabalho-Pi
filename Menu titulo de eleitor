import random
import string

eleitores = []

def gerar_chave():
    caracteres = string.ascii_letters + string.digits
    chave = ""
    for i in range(6):
        chave += random.choice(caracteres)
    return chave

def buscar_eleitor(titulo):
    for eleitor in eleitores:
        if eleitor[0] == titulo:
            return eleitor

def cadastrar():
    titulo = input("Digite o título (12 números): ")

    if len(titulo) != 12 or not titulo.isdigit():
        print("Título inválido❌ ")
        return

eleitor_existente = buscar_eleitor(titulo)
chave = gerar_chave()

if eleitor_existente:
   if eleitor_existente:
      print(" Título já cadastrado✅")

   if eleitor_existente[1] is not None:
        print(" Chave:", eleitor_existente[1])
    else:
      print(" Já votou:", "Sim✅" if eleitor_existente[2] else "Não❌")
      return
 else:
  print("Cadastro ainda não realizado❌")
  print(" Chave:","ainda não foi gerada❌.Finalize o Cadastro")

def votar():
if not eleitor:
   print("Eleitor não cadastrado ❌")
     return
else:
  titulo = input("Digite o título: ")
  chave = input("Digite a chave: ")

if eleitor[1] != titulo:
        print("Titulo incorreta ❌")
        return

  if eleitor[1] != chave:
        print("Chave incorreta ❌")
        return

  if eleitor[2]:
        print("Você já votou ✅")
        return
    
    eleitor[2] = True
    print("Voto registrado com sucesso ✅")

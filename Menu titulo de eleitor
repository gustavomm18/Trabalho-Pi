def buscar_eleitor(titulo):
    for eleitor in eleitores:
        if eleitor[0] == titulo:
            return eleitor

def cadastrar():
    titulo = input("Digite o título (12 números): ")

    if len(titulo) != 12 or not titulo.isdigit():
        print("Título inválido❌ ")
        return

if eleitor_existente:
    print(" Este título já está cadastrado!")

    if eleitor_existente[1] is not None:
        print(" Chave:", eleitor_existente[1])
    else:
        print(" Chave ainda não definida❌")

    print("🗳️ Já votou:", "Sim✅" if eleitor_existente[2] else "Não❌")
    return

